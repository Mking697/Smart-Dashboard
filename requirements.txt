streamlit>=1.40
pandas>=2.0
numpy>=1.26
plotly>=5.20
openpyxl>=3.1
python-dotenv>=1.0
requests>=2.31

# Gemini AI
google-generativeai>=0.8

# Google Sheets live sync (private sheets via service account)
google-auth>=2.30
google-api-python-client>=2.150

# Country name/code normalisation for the world map
pycountry>=24.0

# PDF report export - Kaleido renders charts to PNG (headless browser),
# fpdf2 writes the document. Both avoid the external binaries that
# pdfkit/weasyprint need and that will not install on a small server.
kaleido>=1.0
fpdf2>=2.7
